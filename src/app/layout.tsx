import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  weight: ["400", "500", "600", "700", "800", "900"],
});

export const metadata: Metadata = {
  title: "CapeLoad Logistics | Your Load, Our Road",
  description: "From bikes to trucks, CapeLoad connects you with verified drivers and the right vehicle for every job in the Western Cape.",
  keywords: "logistics, delivery, Cape Town, Western Cape, courier, moving, truck hire",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
